#include "Engine.h"
#include "UploadLvls.h"

/*
TODO:
1. Check if Enemy on the edge of the platform
*/

int main() {

	bool isUpload = 0;

	if (isUpload) {
		startUpload();
	}
	else {
		Engine engine;

		engine.start();
	}

	return 0;
}